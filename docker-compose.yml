version: "3.9"

services:

  bdk:
    build: ./bdk
    container_name: bdk
    stdin_open: true
    tty: true
    ports: 
      - ${BDK_MAP_PORT}:${BDK_MAP_PORT}
      - ${BDK_ELECTRUM_PORT}:${BDK_ELECTRUM_PORT}

  database:
    image: postgres:15
    container_name: database
    volumes:
      - ./data/postgres/pgdata:/var/lib/postgresql/data/pgdata
    ports:
      - ${DATABASE_MAP_PORT}:${DATABASE_SERVER_PORT}
    environment:
      PGDATA: /var/lib/postgresql/data/pgdata
      POSTGRES_DB: ${DATABASE_NAME}
      POSTGRES_USER: ${DATABASE_USER}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD}

  backend:
    build: ./backend
    container_name: backend
    command: python manage.py runserver ${BACKEND_HOST}:${BACKEND_SERVER_PORT}
    volumes: 
      - ./backend/:/backend/
      - ./backend/requirements.txt:/backend/requirements.txt
    ports: 
      - ${BACKEND_MAP_PORT}:${BACKEND_SERVER_PORT}
    depends_on:
      - database
      - bdk
  
  redis:
    image: redis:7.0.5
    container_name: redis
    command: redis-server
    ports:
      - ${REDIS_PORT}:${REDIS_PORT}
    depends_on:
      - backend

  gateway:
    build: ./gateway
    container_name: gateway
    command: python manage.py runserver ${GATEWAY_HOST}:${GATEWAY_SERVER_PORT}
    volumes: 
      - ./gateway/:/gateway/
      - ./backend/requirements.txt:/backend/requirements.txt
    ports: 
      - ${GATEWAY_MAP_PORT}:${GATEWAY_SERVER_PORT}
    environment:
      BACKEND_URL: ${BACKEND_URL}

    depends_on:
      - redis

  frontend:
    build: ./frontend
    container_name: frontend
    command: ng serve --host ${FRONTEND_HOST} --port ${FRONTEND_SERVER_PORT} --disable-host-check
    volumes: 
      - ./frontend:/frontend
      - /frontend/node_modules
      - /frontend/.angular
    ports: 
      - ${FRONTEND_MAP_PORT}:${FRONTEND_SERVER_PORT}
    depends_on:
      - gateway

volumes:
  frontend:
    driver: local
  backend:
    driver: local
  gateway:
    driver: local